/**
 * Provides sound functionality.
 * 
 * The {@link EffectEngine} and {@link MusicEngine} classes are the two most
 * used classes of this package, and {@link DualSoundEngine} combines them both
 * into a single wrapper class.
 */
package yuuki.sound;