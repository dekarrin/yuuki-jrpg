/**
 * Provides sound functionality.
 * 
 * The {@link yuuki.sound.EffectEngine} and {@link yuuki.sound.MusicEngine}
 * classes are the two most used classes of this package, and
 * {@link yuuki.sound.DualSoundEngine} combines them both into a single wrapper
 * class.
 */
package yuuki.sound;