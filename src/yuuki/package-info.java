/**
 * Contains the main game classes. The yuuki package includes the executable
 * {@link Engine} class, which drives all other game components, and the
 * {@link Options} class, which contains all the options for the game.
 */
package yuuki;